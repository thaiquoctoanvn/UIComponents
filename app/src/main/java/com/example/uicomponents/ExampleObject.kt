package com.example.uicomponents

class ExampleObject(imageSource: String, textContent: String, isSelected: Boolean) {

    var imageSource: String = imageSource
    get() = field
    set(value) {
        field = value
    }

    var textContent: String = textContent
    get() = field
    set(value) {
        field = value
    }


    var isSelected: Boolean = isSelected
    get() = field
    set(value) {
        field = value
    }

}